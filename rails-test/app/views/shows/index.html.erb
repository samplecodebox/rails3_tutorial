<h1>Shows page</h1>

<table width="100%" border="1">
  <tr>
    <td> <b> Show name </b> </td>
    <td> </td>
  </tr>
  <% @shows.each do |show| %>
    <tr>
      <td> <%= link_to show.name, show %> </td>
      <td> <%= link_to 'Destroy', show, :method => :delete, :confirm => 'Are you sure?' %> </td>
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New Show', new_show_path %> | 

<%= link_to "List Shows", list_shows_path %>
